---
title: "eGRID PM2.5, NH3, and VOC Emission Estimates"
authors: 
  - "Emma Russell, Abt Global"
  - "Teagan Goforth, Abt Global"
  - "Claire Lay, Abt Global"
freeze: true
format: 
  html:
    toc: true
    toc-expand: true
    toc-depth: 2
    toc-location: left
    html-table-processing: none
    code-fold: true
    embed-resources: true
    self-contained-math: true
execute: 
  message: false
  warning: false
  results: "hold"
  eval: true
params:
  eGRID_year: "2022"

editor: visual
project: 
  execute_dir: project
---

# Overview

This project includes all necessary scripts and documentation to create the Emissions & Generation Resource Integrated Database (eGRID) particulate matter (PM~2.5~), ammonia (NH~3~), and volatile organic compound (VOC) emission estimates.

# Background

eGRID is a comprehensive source of data from [EPA's Clean Air and Power Division (CAPD)](https://epa.gov/power-sector) on the environmental characteristics of almost all electric power generated in the United States. Currently, eGRID includes emissions data on carbon dioxide (CO~2~), nitrogen oxides (NO~X~), sulfur dioxide (SO~2~), methane (CH~4~), and nitrous oxide (N~2~O), but does not include fine particulate matter 2.5 microns in diameter or smaller (PM~2.5~), ammonia (NH~3~), and volatile organic compounds(VOC) emissions. The data used for emission calculations in eGRID is not available for PM~2.5~, NH~3~, and VOCs and is instead collected from the National Emissions Inventory (NEI).

Here we use NEI data to determine PM~2.5~, NH~3~, and VOC emissions and emission rates as well as estimations for units that may not report to the NEI.

The final emissions datasets include four levels of data aggregation:

-   **Unit**: A set of equipment that either produces electricity and is connected to the U.S electricity grid or a set of equipment that is connected to a generator which produces electricity and is connected to the U.S. electricity grid.

-   **Plant**: A facility with one or more units and/or generators that provide power to the electric grid.

-   **State**: U.S. states, Puerto Rico (PR), and the District of Columbia (DC).

-   **eGRID subregion**: EPA defined subregions designed to limit the impacts of the import and export of electricity.

-   **National U.S.**: Contains all 50 states, Puerto Rico (PR), and the District of Columbia (DC).

Further information on the eGRID PM~2.5~, NH~3~, and VOC methodology can be found in the [Particulate Matter Emissions for eGRID2021](https://www.epa.gov/system/files/documents/2024-06/egrid2021-draft-pm-memo.pdf).

The dataset that this code produces is publicly available [here](https://www.epa.gov/egrid/egrid-pm25).

## Using the .qmd file

This is a Quarto (.qmd) file that documents and runs the code necessary to create the eGRID PM~2.5~, NH~3~, and VOC datasets. This document can be used in two ways: First, when used within an IDE, such as RStudio, it serves as an enhanced master script, allowing users to easily perform each step necessary to create the eGRID PM~2.5~, NH~3~, and VOC data from within a single file.

Second, when viewing as a rendered file, it provides thorough documentation of the steps involved in creating the emissions data. When rendered, this file provides additional information and tools for navigating the project. The table of contents provides both a convenient look at the project structure and method for navigating the document. There are also hidden code chunks throughout, marked by a button ("Code" with an arrow next to it). Clicking this button reveals the underlying code from the R script that is being sourced in a given section. Some of the code included in this document is from the eGRID production model while the main infrastructure is specific to PM~2.5~, NH~3~, and VOC emission calculations.

## Data availability

Emissions data and crosswalks used in these calculations are from a version of EPA's NEI that are not publicly available and are therefore not included in this document. Information about the data inputs and the code used to produce the final data are included here to document the process and inform any need for replication.

**If you would like to render this document but do not have access to the data inputs, switch the following parameter in the YAML header under execute:**

*eval: false*

Those that do have access to the data inputs can use this document to produce the eGRID PM~2.5~, NH~3~, and VOC emissions datasets with *eval: true*.

# Install Libraries and Set Parameters

Before loading any data or beginning to construct the eGRID PM~2.5~, NH~3~, and VOC emissions datasets, we install all necessary libraries used in subsequent scripts. Next, the eGRID year is defined, which controls which year of data is loaded from the raw data sources.

## Install required libraries

The script `install_libraries.R` detects all necessary libraries within the project using `renv::dependencies()`, checks for installation, and installs (if needed) and loads into the workspace.

```{r}
#| label: install-libraries.R
#| file: "scripts/1_production_model/install_libraries.R"
#| echo: false
```

```{r}
#| label: load-packages
#| echo: false

library(docstring)
library(dplyr)
library(ggiraph)
library(ggplot2)
library(gt)
library(gtExtras)
library(kableExtra)
library(knitr)
library(patchwork)
library(readxl)
library(stringr)
```

## Define eGRID year

The year of eGRID specified as the parameter "eGRID_year" within the YAML of the Quarto document. When a year value needs to be specified (e.g., when pulling relevant data from the EIA API), it is done so by calling `params$eGRID_year`.

**Current year setting: `r params$eGRID_year`**

## Load all helper functions

There are a set of helper functions used throughout the project. These are defined in the folder `/scripts/functions/`. The `docstring` package is used to provide documentation for functions, similar to typical package documentation. To view the documentation for a given function, run `docstring({function_name})`

Below are the following helper functions specific to the PM~2.5~, NH~3~, and VOC emissions estimates.

```{r}
#| label: load-functions
#| echo: false

# sourcing each file in functions folder
functions_all <- list.files(path = "scripts/functions/")
functions_pm_nh3_voc <- list.files(path = "scripts/functions/", pattern = "*pm_nh3_voc*")
purrr::walk(paste0("scripts/functions/", functions_all), ~ source(.x))
```

::: panel-tabset
### Overview

*Click on the tab to view the helper function names.*

### Helper Functions

```{r}
#| label: list-functions
#| echo: false

# listing function names
kable(stringr::str_remove(stringr::str_remove(functions_pm_nh3_voc, ".R"), "function_"),
      col.names = c("PM2.5, NH3, and VOC Helper Functions")) %>%
            kable_styling(full_width = F)
```
:::

# Input Data

## NEI

NEI provides emission estimates for PM~2.5~, NH~3~, and VOC pollutants using the Emissions Inventory System (EIS) that aggregates pollutant data from a variety of state, local, and tribal data sources. A version of this data is provided to us annually while a publicly-available version is released every three years. You can [read more](https://www.epa.gov/air-emissions-inventories/national-emissions-inventory-nei) about NEI data and [download](https://www.epa.gov/air-emissions-inventories/get-air-emissions-data-0) the publicly-available version of data.

*NOTE: The publicly-available NEI-EIA data may differ from the version used to produce the finalized eGRID PM~2.5~, NH~3~, and VOC data.*

## EPA

The EPA's Clean Air and Power Division (CAPD) contains power plant emissions, compliance, and allowance data. We create and incorporate a composite file from several CAPD sources, containing data about facilities and annual emissions. Specifically, we include data about facility attributes, annual emissions, and annual emissions during ozone months.

These data are available through the [CAPD API](https://www.epa.gov/power-sector/cam-api-portal#/documentation). `data_load_epa.R` downloads the facility attributes and emissions data for a selected year from the CAPD API. These raw files are stored in a new folder called `data/1_production_model/raw_data/epa/` and will be combined and cleaned in subsequent steps. For this script to run, an API key is required.

An API key can be requested [here](https://www.epa.gov/power-sector/cam-api-portal#/api-key-signup). Once a key is obtained, paste your key into the file `api_keys/epa_api_key.txt` and save. Once saved, the script `data_load_epa.R` will be able to successfully connect to and load data from the CAPD API.

```{r}
#| label: load-data-epa 
#| file: "scripts/1_production_model/data_load_epa.R" 
#| echo: fenced 
#| error: false 
```

Three data tables are downloaded from the the CAPD and stored into one .RDS file, `epa_raw.RDS`. This file consists of facility attributes, annual emissions data, and containing annual emissions data for the ozone months. See a summary of the raw EPA file below, separated into categories.

### EPA Raw Tables

```{r}
#| label: create-epa-tables 
#| echo: false  

source("scripts/functions/function_summary_table.R") 

# function to create summary table  
epa_raw <- read_rds(glue::glue("data/1_production_model/raw_data/epa/{params$eGRID_year}/epa_raw.RDS")) 

table_epa_raw <- create_summary_table(epa_raw)  

epa_facilities      <- epa_raw[1:26] 
epa_emissions       <- epa_raw[27:46] 
epa_emissions_ozone <- epa_raw[47:58]  

table_facilities      <- create_summary_table(epa_facilities) 
table_emissions       <- create_summary_table(epa_emissions) 
table_emissions_ozone <- create_summary_table(epa_emissions_ozone)
```

::: panel-tabset
#### Overview

*Click through the tabs above to view files represented from each EPA source.*

**EPA facility attributes**: Columns 1 - 26 of `epa_raw.RDS`.

**EPA annual emissions**: Columns 27 - 47 of `epa_raw.RDS`.

**EPA ozone season emissions**: Columns 47 - 58 of `epa_raw.RDS`. The ozone season is defined as May through September.

#### EPA facility attributes

```{r}
#| label: print-epa-tab-facilities 
#| echo: false  

table_facilities
```

#### EPA annual emissions

```{r}
#| label: print-epa-tab-emissions 
#| echo: false  

table_emissions
```

#### EPA ozone season emissions

```{r}
#| label: print-epa-tab-emissions-ozone 
#| echo: false  

table_emissions_ozone
```
:::

## EIA

The U.S. Energy Information Administration (EIA), a part of the Department of Energy, collects and maintains energy-related data for policy making and for the public. As of 2024, the EIA API does not contain the most detailed data available, which is necessary for the construction of eGRID. More detailed data for the EIA forms used in the eGRID production model - [923](https://www.eia.gov/electricity/data/eia923/), [860](https://www.eia.gov/electricity/data/eia860/), and [861](https://www.eia.gov/electricity/data/eia861/) - are available as zipped excel file downloads on the EIA website.

The EIA-923 form provides control efficiency information used to adjust PM~2.5~, NH~3~, and VOC emission estimates\*. If the EIA has not been previously downloaded during the production model `data_load_eia.R` downloads and unzips the zip file for each form within `data/1_production_model/raw_data/`. Each excel file contains several sheets that serve as the raw EIA data sources used in producing the eGRID data.

*\*Control efficiency data is currently only available for PM~2.5~ emissions*

```{r}
#| label: data-load-eia
#| file: "scripts/1_production_model/data_load_eia.R"
#| error: false
#| echo: fenced
```

The table within EIA Files and Sheets displays each of the unzipped raw files from EIA-923, the only form needed in PM~2.5~, NH~3~, and VOC emission calculations.

```{r}
#| label: custom-gt-print
#| echo: false

knit_print.gt <- function(x, ...) {
  stringr::str_c("<div style='all:initial;'>\n", gt::as_raw_html(x), "\n</div>") %>%
    knitr::asis_output()
}
registerS3method("knit_print", "gt_tbl", knit_print.gt, envir = asNamespace("gt"))
```

```{r}
#| label: create-eia-sheets-list

sheets_923  <- get_sheets("923")
tab_923  <- make_sheets_table(sheets_923, "923") 
 
sheets_used923 <-   c("Page 1 Generation and Fuel Data", # eia-923
                      "Page 1 Puerto Rico",
                      "Page 3 Boiler Fuel Data",
                      "Page 4 Generator Data",
                      "8C Air Emissions Control Info")

```

#### EIA Files and Sheets {#eia-files-sheets}

::: panel-tabset
#### Overview

*Click on the tab to view the EIA-923 data.*

#### EIA-923

**EIA-923**: Data reported on fuel consumption and generation

```{r}
#| label: print-eia-923-sheets
#| tbl-cap: "EIA-923 sheets"
#| echo: false

tab_923
```
:::

## NEI-EIA Crosswalk

EPA's Office of Air Quality Planning and Standards (OAQPS) produces an NEI-EIA crosswalk used to match the electric generating units from the NEI emissions data to the eGRID data. NEI uses EIS codes to identify units while eGRID uses Office of Regulatory Information Systems Plant (ORISPL) codes. The crosswalk matches EIS and ORISPL codes of the same unit to directly link eGRID units to their NEI emissions data if available.

*NOTE: The NEI-EIA crosswalk is not publicly available.*

::: panel-tabset
#### Overview

*Click on the tab to view variables contained in the NEI-EIA Crosswalk*

#### NEI-EIA Crosswalk

```{r}
#| label: create-xwalk-summary
#| echo: false

xwalk_path <- glue::glue("data/2a_pm_nh3_voc/inputs/nei_eia_crosswalk/{params$eGRID_year}/xwalk_nei_eia.csv")

nei_eia_xwalk <-
  read.csv(xwalk_path)

create_summary_table(nei_eia_xwalk)
```
:::

## Emissions Factors

EPA's AP-42 form reports emissions factors and is used in part to estimate PM~2.5~, NH~3~, and VOC emissions. These emissions factors are specific to unit's fuel, firing type, and prime mover and when necessary, are adjusted to account for a control efficiency. Emission factors for each of the emission types are provided as static tables.

Below are the column names within the PM~2.5~ emissions factors static table and are the same within the NH~3~ and VOC tables.

::: panel-tabset
#### Overview

*Click on the tab to view PM~2.5~, NH~3~, and VOC emissions factors static table variables.*

#### PM~2.5~ EFs

```{r}
#| label: create-pm-emission-factors-summary
#| echo: false

ef_pm_path <- glue::glue("data/2a_pm_nh3_voc/static_tables/emission_factors_pm.csv")

efs_pm <-
  read.csv(ef_pm_path)

create_summary_table(efs_pm)
```

#### NH~3~ EFs

```{r}
#| label: create-nh3-emission-factors-summary
#| echo: false

ef_nh3_path <- glue::glue("data/2a_pm_nh3_voc/static_tables/emission_factors_nh3.csv")

efs_nh3 <-
  read.csv(ef_nh3_path)

create_summary_table(efs_nh3)
```

#### VOC EFs

```{r}
#| label: create-voc-emission-factors-summary
#| echo: false

ef_voc_path <- glue::glue("data/2a_pm_nh3_voc/static_tables/emission_factors_voc.csv")

efs_voc <-
  read.csv(ef_voc_path)

create_summary_table(efs_voc)
```
:::

# Clean Raw Data

## EPA

There are several procedures applied to the raw EPA file:

-   Variable name standardization

    -   All variable names are converted to snake case (e.g., "snake_case").

    -   Each form includes identifiers such as a given plant name, prime mover, fuel type, etc., but the assigned column names are inconsistent. To facilitate data operations (e.g., joins) and reduce confusion, we use a common naming scheme across all files (including EIA and EPA).

        -   `plant_id`

        -   `plant_name`

        -   `plant_state`

        -   `prime_mover`

        -   `fuel_type`

        -   `generator_id`

        -   `boiler_id`

        -   `nameplate_capacity`

-   Removing unnecessary plants and columns

    -   Plants listed as future, retired, or long-term cold storage are removed. Additionally, plants with IDs above 80000 are removed

-   Create source variables and apply source: `EPA/CAPD`

    -   `heat_input_source`

    -   `heat_input_oz_source`

    -   `nox_source`

    -   `nox_oz_source`

    -   `so2_source`

    -   `co2_source`

    -   `hg_source`

-   Re-coding values to standardized abbreviations

    -   Ex. Operating Status to OP, unit type description to unit type abbreviation

-   Removing unnecessary notes about start date

    -   This keeps all data rows into a usable, consistent format. In the raw version, some plants have added notes about dates or the plant.

```{r}
#| label: data-clean-epa 
#| file: "scripts/1_production_model/data_clean_epa.R" 
#| results: hold
```

::: panel-tabset
#### Overview

*Click tab above to view variables contained in* `epa_clean.RDS`.

#### EPA clean

```{r}
#| label: create-epa-summary-table 
#| echo: false  

create_summary_table(readr::read_rds(glue::glue("data/1_production_model/clean_data/epa/{params$eGRID_year}/epa_clean.RDS")))
```
:::

## EIA

From the raw Excel downloads, we load, clean, and save select files that are used in eGRID production. The EPA data is used to produce three "clean" EIA files:

-   `eia_923_clean.RDS`

-   `eia_860_clean.RDS`

-   `eia_861_clean.RDS`

Each of these .RDS files contains lists of the relevant tables (stored as dataframes) from each EIA form.

There are several procedures that are applied to each of the raw EIA files:

-   Handling Excel format

    -   Each Excel file contains header rows of varying lengths. These rows are skipped when read in.

    -   Files contain various missing value characters, including: " ","X", and ".". These characters are converted to explicit missing values (i.e., "NA")

-   Variable name standardization

    -   The same method as EPA data above.

```{r}
#| label: data-clean-eia
#| file: "scripts/1_production_model/data_clean_eia.R"
#| results: hold
```

```{r}
#| label: load-eia-clean-files
#| echo: false

eia_923_files <- read_rds(
        glue::glue("data/1_production_model/clean_data/eia/{params$eGRID_year}/eia_923_clean.RDS"))
```

### EIA-923

EIA-923 is the only form needed to produce PM~2.5~, NH~3~, and VOC emissions data

::: panel-tabset
#### Overview

*Click through the tabs above to preview values represented from required EIA-923 files.*

```{r}
#| label: eia-923-summary-tabs
#| results: asis

tabs_923 <- 
  eia_923_files %>% 
  map(., ~ create_summary_table(.x))

purrr::iwalk(tabs_923, ~ {
  cat("#### ", .y, "\n\n")
  
  print(.x)
  
  cat("\n\n")
} )
```
:::

# Produce Data

## Unit Files

For each emissions type (PM~2.5~, NH~3~, and VOC), the unit file computes annual emissions and emissions rates for units and is combined with data from the eGRID production model.

For full documentation, reference the [Particulate Matter Emissions for eGRID2021](https://www.epa.gov/system/files/documents/2024-06/egrid2021-draft-pm-memo.pdf).

```{r}
#| label: unit-files-create
#| file: "scripts/2a_pm_nh3_voc/unit_file_create_pm_nh3_voc.R"
#| message: FALSE
```

```{r}
#| label: unit-file-table
#| include: false

unit_file_pm <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/unit_file_pm.RDS"))

unit_file_nh3 <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/unit_file_nh3.RDS"))

unit_file_voc <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/unit_file_voc.RDS"))
```

### View Unit Files Data

::: panel-tabset
#### Overview

*Click through the tabs above to preview data contained within the unit files.*

#### PM~2.5~ Unit File

```{r}
#| label: pm-unit-file-summary

create_summary_table(unit_file_pm)
```

#### NH~3~ Unit File

```{r}
#| label: nh3-unit-file-summary

create_summary_table(unit_file_nh3)
```

#### VOC Unit File

```{r}
#| label: voc-unit-file-summary

create_summary_table(unit_file_voc)
```
:::

## Plant Files

The plant file aggregates the unit data at the plant level using the eGRID production model dataset and calculates unadjusted and adjusted annual emissions, and input and output emissions rates.

```{r}
#| label: plant-files-create
#| file: "scripts/2a_pm_nh3_voc/plant_file_create_pm_nh3_voc.R"
#| message: FALSE
```

```{r}
#| label: plant-file-table
#| include: false

plant_file_pm <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/plant_file_pm.RDS"))

plant_file_nh3 <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/plant_file_nh3.RDS"))

plant_file_voc <- readr::read_rds(
           glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/plant_file_voc.RDS"))
```

### View Plant Files Data

::: panel-tabset
#### Overview

*Click through the tabs above to preview data contained within the plant files.*

#### PM~2.5~ Plant File

```{r}
#| label: pm-plant-file-summary

create_summary_table(plant_file_pm)
```

#### NH~3~ Plant File

```{r}
#| label: nh3-plant-file-summary

create_summary_table(plant_file_nh3)
```

#### VOC Plant File

```{r}
#| label: voc-plant-file-summary

create_summary_table(plant_file_voc)
```
:::

## Region Aggregation Files

The plant file is aggregated to calculate annual net generation, annual emissions, and output emissions rates from the plant-level data to the various aggregated levels: state (ST), eGRID subregion (SRL), and US level.

```{r}
#| label: regional-aggregation-files-create
#| file: "scripts/2a_pm_nh3_voc/region_aggregation_create_pm_nh3_voc.R"
#| message: FALSE
```

```{r}
#| label: load-reg-agg-files
#| echo: false

pm_st_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/state_aggregation_pm.RDS"))

pm_srl_agg <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/subregion_aggregation_pm.RDS"))

pm_us_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/us_aggregation_pm.RDS"))

nh3_st_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/state_aggregation_nh3.RDS"))

nh3_srl_agg <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/subregion_aggregation_nh3.RDS"))

nh3_us_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/us_aggregation_nh3.RDS"))

voc_st_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/state_aggregation_voc.RDS"))

voc_srl_agg <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/subregion_aggregation_voc.RDS"))

voc_us_agg  <- read_rds(
        glue::glue("data/2a_pm_nh3_voc/outputs/{params$eGRID_year}/us_aggregation_voc.RDS"))
```

### View Aggregation Files Data

Below are summary tables for the state, subregion, and U.S. aggregated data for each emissions type.

::: panel-tabset
#### Overview

*Click through the tabs above to preview PM~2.5~, NH~3~, and VOC data for each region aggregation.*

**ST**: State Aggregation

**SRL**: Subregion Aggregation

**US**: U.S. Aggregation

#### PM~2.5~ ST

```{r}
#| label: pm-st-agg-summary

create_summary_table(pm_st_agg)
```

#### PM~2.5~ SRL

```{r}
#| label: pm-srl-agg-summary

create_summary_table(pm_srl_agg)
```

#### PM~2.5~ US

```{r}
#| label: pm-us-agg-summary

create_summary_table(pm_us_agg)
```

#### NH~3~ ST

```{r}
#| label: nh3-st-agg-summary

create_summary_table(nh3_st_agg)
```

#### NH~3~ SRL

```{r}
#| label: nh3-srl-agg-summary

create_summary_table(nh3_srl_agg)
```

#### NH~3~ US

```{r}
#| label: nh3-us-agg-summary

create_summary_table(nh3_us_agg)
```

#### VOC ST

```{r}
#| label: voc-st-agg-summary

create_summary_table(voc_st_agg)
```

#### VOC SRL

```{r}
#| label: voc-srl-agg-summary

create_summary_table(voc_srl_agg)
```

#### VOC US

```{r}
#| label: voc-us-agg-summary

create_summary_table(voc_us_agg)
```
:::

# Produce Final Datasets

For each emissions type, the data is formatted and added to the previous year's data in an Excel file that contains all five levels of data aggregation. For headers, we add descriptions and label conventions to maintain consistency with the eGRID production model. Graphs of subregion-level generation, emissions, and emission rates are included in the final file.

The final output files are:

-   `r glue::glue("egrid{params$eGRID_year}_pm_emissions.xlsx")`
-   `r glue::glue("egrid{params$eGRID_year}_nh3_emissions.xlsx")`
-   `r glue::glue("egrid{params$eGRID_year}_voc_emissions.xlsx")`

```{r}
#| label: final-formatting
#| file: "scripts/2a_pm_nh3_voc/final_formatting_pm_nh3_voc.R"
#| message: FALSE
#| results: FALSE
```

## View Subregion-Level Graphs

Below are the `r {params$eGRID_year}` PM~2.5~, NH~3~, and VOC graphs included in the final datasets.

::: panel-tabset
#### Overview

*Click through the tabs above to view subregion graphs for each emissions type.*

#### PM~2.5~ Annual Generation

```{r}
#| label: pm-annual-generation-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/pm_annual_generation_{params$eGRID_year}.png"))

```

#### PM~2.5~ Emissions

```{r}
#| label: pm-emissions-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/pm_emissions_{params$eGRID_year}.png"))
```

#### PM~2.5~ Emission Rates

```{r}
#| label: pm-emissions-rate
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/pm_rate_{params$eGRID_year}.png"))
```

#### NH~3~ Annual Generation

```{r}
#| label: nh3-annual-generation-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/nh3_annual_generation_{params$eGRID_year}.png"))

```

#### NH~3~ Emissions

```{r}
#| label: nh3-emissions-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/nh3_emissions_{params$eGRID_year}.png"))
```

#### NH~3~ Emission Rates

```{r}
#| label: nh3-emissions-rate
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/nh3_rate_{params$eGRID_year}.png"))
```

#### VOC Annual Generation

```{r}
#| label: voc-annual-generation-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/voc_annual_generation_{params$eGRID_year}.png"))

```

#### VOC Emissions

```{r}
#| label: voc-emissions-plot
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/voc_emissions_{params$eGRID_year}.png"))
```

#### VOC Emission Rates

```{r}
#| label: voc-emissions-rate
#| echo: FALSE

knitr::include_graphics(glue::glue("data/2a_pm_nh3_voc/static_tables/formatting/voc_rate_{params$eGRID_year}.png"))
```
:::
